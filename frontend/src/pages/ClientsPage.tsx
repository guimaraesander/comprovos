export function ClientsPage() {
  return (
    <section>
      <h2 style={{ marginTop: 0 }}>Clientes</h2>
      <p style={{ color: "#64748b" }}>
        Tela inicial de clientes. Próximo passo: listar e cadastrar clientes via API.
      </p>
    </section>
  );
}